#include <iostream>

using namespace std;

int main(){
    // declarar un arreglo int de 5 posiciones
    int cantidades[5];

    // declarar un arreglo float de 6 posiciones e inicializarlo.
    //float pagos[];

    // asignar valores especificos al arreglo de enteros

    // escribir en los valores del arreglo pagos utilizando un ciclo for

    // leer un nuevo valor y sumarl al valor de la tercera posicion del arreglo cantidades

    // incrementar los valores del arreglo pagos en un 10%, utilizar ciclo for

    // determinar el tamaño en bytes de ambos arreglos y de un elemento

    // utilice un ciclo while para mostrar que elementos de cantidades son par
    // debe detener el ciclo al encontrar un numero impar
    
    return 0;
}